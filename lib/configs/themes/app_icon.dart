import 'package:flutter/material.dart';
import 'package:timebox/configs/themes/app_colors.dart';

class AppIcon {
  static const Icon back = Icon(
    Icons.arrow_back_ios,
    size: 20,
    color: AppColors.black,
  );
  static const Icon next = Icon(
    Icons.arrow_forward_ios,
    size: 20,
    color: AppColors.black,
  );
  static const Icon down = Icon(
    Icons.keyboard_arrow_down,
    size: 20,
    color: AppColors.black,
  );
  static const Icon backCalendar = Icon(
    Icons.arrow_back_ios,
    size: 15,
    color: AppColors.black,
  );
  static const Icon forwardCalendar = Icon(
    Icons.arrow_forward_ios,
    size: 15,
    color: AppColors.black,
  );
}
